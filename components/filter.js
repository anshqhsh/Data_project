// 메뉴에 맞는 filter값을 받아와서 실행한다.
// 각 필터 조건에 맞는 값을 받아온다.
// 성별, 나이, 인종, 민족, 사망여부

const Filter = ({ mutate }) => {
  console.log({ mutate });
  return (
    <>
      <span>filter</span>
    </>
  );
};

export default Filter;
